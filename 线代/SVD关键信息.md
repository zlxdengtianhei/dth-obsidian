该模型通过以下步骤改进了这一点：

1. **特征提取**：首先，它从输入矩阵 `H` 中提取一组“结构性”特征。这里选择的是基于**Gram矩阵** (`H * H^H` 和 `H^H * H`) 的特征，因为Gram矩阵的对角线元素和范数与原始矩阵 `H` 的能量分布和奇异值大小密切相关。

还是要使用模块化的训练策略

理论上，训练这样一个简单网络好像是不用太多epoch，如果30 50 没提升太多就可能之后也没什么提升了

loss 震荡不收敛，更多还是要调整loss的公式，调整不同部分之间的参数，以及学习率，batchsize

还是不能太过相信ai的是否相同的判断，要仔细的对比，另外优化方面还是要从架构上，问ai多考虑，多实验，loss函数的设置可能只是一部分

特征还真不是越多越好，loss设计方面还是要看一下数值大小，哪个重要

网络中的

训练要记得打印出loss的所有组成部分，print到屏幕与log